{{one-way-input
title
update=(action (mut title))
keyEvents=(hash
13=(action 'onEnter')
)
}}

<ul>
  {{#each books as |book|}}
  <li>{{ book.title }}</li>
  <button {{action removeBook book}}>Remove</button>
  {{/each}}
</ul>

{{yield}}